// use std::thread;
// use std::net::TcpStream;
// /// connect tcp, use two spawn
// fn connect_tcp(addr:String){
//     let mut stream = TcpStream::connect(addr).expect("connect tcp err");
    

// }